{% extends "base.html.twig" %}

{% block body %}

<div class="container mt-5 bg-white shadow p-5">
	<h1 class="text-center mb-5">Créer un nouveau plat</h1>
	{{ form_start(dishes, {'attr': {'class': 'row g-3 justify-content-center'}}) }}
		{{ form_row(dishes.categories, {'attr': {'class': 'form-select mb-2'}}) }}
		{{ form_row(dishes.title, {'attr': {'class': 'form-control mb-2'}}) }}
		{{ form_row(dishes.description, {'attr': {'class': 'form-control mb-2', 'rows': 5}}) }}
		{{ form_row(dishes.price, {'attr': {'class': 'form-control'}}) }}
		<div class="col-12">
			<input type="submit" class="btn btn-primary" value="Publier la carte">
		</div>
	{{ form_end(dishes) }}
</div>

{% endblock %}